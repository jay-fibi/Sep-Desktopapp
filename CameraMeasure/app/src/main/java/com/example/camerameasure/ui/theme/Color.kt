package com.example.camerameasure.ui.theme

import androidx.compose.ui.graphics.Color

/** Brand colours used by the app theme. */
val MeasureBlue = Color(0xFF1565C0)
val MeasureBlueDark = Color(0xFF0B3D6B)
val MeasureAmber = Color(0xFFFFB300)
val MeasureSurfaceLight = Color(0xFFF6F7FB)
val MeasureSurfaceDark = Color(0xFF11151C)

/** Colour of the calibration (reference) overlay. */
val CalibrationColor = Color(0xFF00E5FF)

/** Repeating palette used to tell the measurements apart on the photo. */
val MeasurementColors = listOf(
    Color(0xFFFFB300), // amber
    Color(0xFF76FF03), // lime
    Color(0xFFFF4081), // pink
    Color(0xFFB388FF), // violet
    Color(0xFFFF6E40), // deep orange
    Color(0xFF40C4FF)  // light blue
)
