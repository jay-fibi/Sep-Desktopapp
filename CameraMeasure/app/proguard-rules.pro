# Add project specific ProGuard rules here.
# The app ships with minification disabled, so these rules are only used if
# `isMinifyEnabled` is turned on for the release build type.

# Keep CameraX / Compose defaults; both libraries ship their own consumer rules.
-dontwarn org.jetbrains.annotations.**
